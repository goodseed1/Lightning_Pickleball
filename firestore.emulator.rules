rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Emulator rules: Allow all reads and writes for testing
    // This is ONLY for testing purposes
    match /{document=**} {
      allow read, write: if true;
    }
  }
}